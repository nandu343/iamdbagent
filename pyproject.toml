[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "iamdbagent"
version = "0.1.0"
description = "Agentic IAM Governance — Phase1 ingestion"
authors = [ { name = "You" } ]

